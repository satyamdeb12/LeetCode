                }
            }
        }
        int[] intersectionArray = new int[result.size()];
        int index = 0;
        for (int num : result) {
            intersectionArray[index++] = num;
        }

        return intersectionArray;
    }
}
[
